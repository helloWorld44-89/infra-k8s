apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: job-ops-claim0
  name: job-ops-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
